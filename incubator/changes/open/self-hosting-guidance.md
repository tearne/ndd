# Map hosts its own guidance

**Mode:** (TBD)

## Intent

The map should hold map and process guidance within itself — e.g. the very node character limit we keep forgetting — rather than in separate docs. This map is going to be how we capture the process, so the guidance that governs authoring and process belongs on the map too, where it is seen in context.
