# Testing in the map

## Intent

Decide how testing is specified in the map: where a node states what must hold and how it is checked, and whether a reference implementation can sit beside the prose. The Fingerprint node is the first candidate — every agent must produce the same hash for the same node, which is a property that wants a runnable check rather than a description.

Parked via aside during change 80's build (2026-09-10); numbered 275 to run just before go-live.
