# Navigation above scaffolding

**Cadence:** Wander

## Intent

_(Approved 2026-09-11. Build started the same day.)_

Move each node's navigation links above its yaml scaffolding block, so a node reads heading, links, block, prose. GitHub renders the block as a code box, and links first keeps the box from sitting between the heading and the way onward. Node and Node Identity say the order; every node in both files is re-typeset.

## Log

- 2026-09-11 — 67 nodes re-typeset; order, links and reciprocity verified; dist rebuilt and reinstalled.

## Conclude

_(Approved 2026-09-11.)_

Every node in both files re-typeset with navigation links above the scaffolding block; Node and Node Identity state the order. No other rule needed changing, since Fingerprint drops both parts regardless of order.
